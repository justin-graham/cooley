ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/corporate_audit

# Comma-separated list of origins allowed to send cookies
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Auth/session settings
SESSION_EXPIRE_MINUTES=1440

# Parser hard timeout per file (seconds)
PARSER_TIMEOUT_SECONDS=45

# Set to 1 for local HTTP development (disables secure cookie flag)
LOCAL_DEV=1
